// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * LogService searches, enumerates, and exports persisted log entries for the
 * Logs page.
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * Bind wires the LogService to an open library's catalog in place.
 */
export function Bind(core: $models.AppCore | null): $CancellablePromise<void> {
    return $Call.ByID(3479872355, core);
}

/**
 * Export streams all log entries matching the filters (ignoring pagination) to a
 * user-chosen file in json or csv format via a native save dialog and returns
 * the written path. It returns "" when the user cancels the dialog. Rows are
 * pulled from the DB in pages (logExportPageSize) and written to disk one at a
 * time — CSV row-by-row, JSON as a streamed array — so memory stays flat on a
 * large log, and log:export-progress (rows written) is emitted throttled.
 */
export function Export(query: string, level: string, subsystem: string, fromISO: string, toISO: string, format: string): $CancellablePromise<string> {
    return $Call.ByID(3153149562, query, level, subsystem, fromISO, toISO, format);
}

/**
 * Search returns a page of log entries matching the filters, newest first.
 */
export function Search(query: string, level: string, subsystem: string, fromISO: string, toISO: string, page: number, pageSize: number): $CancellablePromise<$models.PageResult<$models.LogEntryDTO>> {
    return $Call.ByID(3240393328, query, level, subsystem, fromISO, toISO, page, pageSize);
}

/**
 * SetGate injects the shared library gate. Called once by main.go after
 * construction; never called in unit tests (leaving the service ungated).
 */
export function SetGate(gate: $models.LibraryGate | null): $CancellablePromise<void> {
    return $Call.ByID(892112739, gate);
}

/**
 * Subsystems returns the distinct subsystem names present in the log, sorted.
 */
export function Subsystems(): $CancellablePromise<string[] | null> {
    return $Call.ByID(2519064772);
}
