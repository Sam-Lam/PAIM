import { Placeholder } from "../components";

// Phase 2: HistoryService.ListSessions / SessionEvents — sessions table with
// per-session event drill-down and copy/adopt mode badges.
export function HistoryPage() {
  return (
    <Placeholder
      title="Import History"
      description="Every import session with its outcome, counters, and per-file event log."
    />
  );
}
