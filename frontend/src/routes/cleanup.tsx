import { Placeholder } from "../components";

// Phase 2: CleanupService.PickFolder / Analyze — read-only folder analysis with
// per-class breakdown and a safe-delete recommendation.
export function CleanupPage() {
  return (
    <Placeholder
      title="Cleanup Assistant"
      description="Analyze any folder and get a safe-to-delete recommendation with the reasons."
    />
  );
}
