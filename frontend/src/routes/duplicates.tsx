import { Placeholder } from "../components";

// Phase 2: DuplicateService.ListDuplicates / ResolveDuplicate — side-by-side
// duplicate pairs with keep/delete/move actions (via ConfirmDialog).
export function DuplicatesPage() {
  return (
    <Placeholder
      title="Duplicate Manager"
      description="Full-hash-confirmed duplicate pairs, with safe resolution actions."
    />
  );
}
