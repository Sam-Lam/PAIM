import { Placeholder } from "../components";

// Phase 2: ProviderService.AvailablePlugins / List / Add / Update — configure
// backup destinations (starting with localfs).
export function ProvidersPage() {
  return (
    <Placeholder
      title="Providers"
      description="Configure backup destinations and their plugins."
    />
  );
}
