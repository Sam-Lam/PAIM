import { Placeholder } from "../components";

// Phase 2: SettingsService.GetAll / Update — Master Library root, backup worker
// count, retry limits, default event name, and metadata availability.
export function SettingsPage() {
  return (
    <Placeholder
      title="Settings"
      description="Master Library location, backup workers, retries, and defaults."
    />
  );
}
