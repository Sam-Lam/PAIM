import { Placeholder } from "../components";

// Phase 2: BackupService.ListJobs / QueueSummary / Pause / Resume / Retry /
// Cancel — the persisted backup job queue with worker controls.
export function BackupQueuePage() {
  return (
    <Placeholder
      title="Backup Queue"
      description="The persisted backup job queue — pause, resume, retry, and cancel work."
    />
  );
}
