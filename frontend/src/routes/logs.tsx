import { Placeholder } from "../components";

// Phase 2: LogService.Search / Subsystems / Export — searchable, filterable log
// viewer with JSON/CSV export.
export function LogsPage() {
  return (
    <Placeholder
      title="Logs"
      description="Search, filter, and export the structured application log."
    />
  );
}
