// Placeholder shell. Phase 5 replaces this with the full TanStack Router UI
// (Dashboard, Import, Sources, History, Duplicates, Cleanup, Backup Queue,
// Providers, Logs, Settings). Kept minimal so the frontend compiles against the
// generated service bindings.
function App() {
  return <div>PAIM</div>;
}

export default App;
