# Photo Archive Integrity Manager (PAIM) — Architecture Specification

This document is the single source of truth for all implementation work. Do not deviate
from decisions recorded here. If something is genuinely ambiguous, prioritize
**data integrity > recoverability > performance**, and **explicit feedback > silent behavior**.

PAIM is a macOS desktop app for photo/video **import, archive, verification, backup, and
storage reclamation**. It is NOT a DAM. It never risks data loss; every operation is
restart-safe.

## Stack (fixed — do not change)

- Backend: Go 1.24+, GORM + SQLite (`gorm.io/driver/sqlite`, cgo), BLAKE3
  (`lukechampine.com/blake3`), exiftool subprocess (preferred; graceful degradation if
  missing), `fsnotify`, `github.com/google/uuid`, `howett.net/plist` for diskutil output.
- Frontend: Wails **v3** (alpha2.117), React + TypeScript + Vite, Tailwind CSS v4
  (via `@tailwindcss/vite`), TanStack Router, TanStack Table, Heroicons. Dark mode default.
- Module path: `github.com/autolinepro/paim`.

## Package layout

```
main.go                          # Wails app entry: wires everything, registers services
internal/
  domain/       # GORM models, enums, status constants. No business logic. No imports of other internal pkgs.
  db/           # Open() -> *gorm.DB with WAL, foreign_keys=ON, busy_timeout; AutoMigrate; indexes
  repo/         # Repositories over GORM (AssetRepo, SessionRepo, SourceRepo, BackupRepo, LogRepo, SettingsRepo)
  logging/      # slog handler that tees to console + Logs table (async, never blocks callers)
  hashing/      # BLAKE3 quick hash + full hash + streaming file hash
  mediatype/    # extension registry: media kind (photo/video/raw/sidecar), Live Photo pairing rules
  metadata/     # exiftool batch wrapper (-stay_open), JSON parse -> domain.AssetMetadata
  source/       # ImportSource interface, identification, content fingerprint, confidence model
  volumes/      # mounted-volume watcher (/Volumes via fsnotify) + diskutil/system_profiler collectors
  archive/      # destination layout resolution (YYYY/YYYY-MM-DD Event/[RAW/]), collision handling
  importer/     # scan, dry-run, import pipeline (copy->verify->record), resume, cancellation
  backup/       # job queue (SQLite-persisted), worker pool, pause/resume/retry/cancel
    plugins/localfs/   # first BackupPlugin: local filesystem destination
  cleanup/      # Cleanup Assistant: read-only folder analysis + safe-delete rules
  services/     # Wails-bound service structs (thin: validate, call engines, emit events)
frontend/src/
  main.tsx      # TanStack Router setup
  routes/       # one file per page: dashboard, import, sources, history, duplicates, cleanup, backup-queue, providers, logs, settings
  components/   # Sidebar, StatusBadge, ProgressBar, DataTable (TanStack Table wrapper), ConfirmDialog, EmptyState
  lib/          # format helpers, useWailsEvent hook, query helpers
  bindings/     # GENERATED by `wails3 generate bindings` — never hand-edit
```

Dependency direction: `services -> importer/backup/cleanup/source -> repo -> domain`.
`domain` imports nothing internal. Engines take interfaces (defined where consumed) so they
are testable without SQLite or the filesystem where practical.

## Database

SQLite via GORM. On open (in `internal/db`):
- `PRAGMA journal_mode=WAL`, `PRAGMA foreign_keys=ON`, `PRAGMA busy_timeout=5000`,
  `PRAGMA synchronous=NORMAL` (durability + WAL performance).
- AutoMigrate all models; explicit indexes on all hash columns and foreign-key ID columns.
- **Never hard-delete rows.** All models embed `SoftDelete` (a `DeletedAt gorm.DeletedAt` +
  `Deleted bool` flag). "Delete" flows set the flag/soft-delete only.
- DB file lives in `~/Library/Application Support/PAIM/paim.db` (overridable for tests via
  `db.Open(path)`).

### Models (internal/domain) — field names are canonical

All UUIDs are `string` (uuid v4), primary key `ID`. All models have `CreatedAt`, `UpdatedAt`.

**Asset**: ID, OriginalFilename, OriginalExtension (lowercase, no dot), OriginalFullPath,
SourceID (FK ImportSource), SessionID (FK ImportSession), QuickHash (indexed), FullHash
(indexed, nullable — only computed on quick-hash collision or verification need),
FileSize (int64), CaptureDate (*time.Time), ImportDate, MediaType (enum: photo, raw_photo,
video, live_photo_pair), Width, Height, DurationSeconds (float64), CameraMake, CameraModel,
Lens, ISO (int), ShutterSpeed (string), Aperture (string), GPSLatitude/GPSLongitude
(*float64), CurrentArchivePath, VerificationStatus (enum: pending, verifying, verified,
failed), BackupStatus (enum: none, pending, partial, complete, failed), DuplicateOfAssetID
(*string, FK Asset), LivePhotoPartnerID (*string), Deleted flag + DeletedAt.

**ImportSession**: ID, StartedAt, CompletedAt (*time.Time), DurationSeconds, SourceID,
DestinationRoot, FilesScanned, FilesImported, Duplicates, Failures, Skipped, Status
(enum: scanning, dry_run, running, paused, completed, failed, cancelled, interrupted), Notes.

**ImportSource**: ID, SourceType (enum: sd_card, usb_ssd, external_hdd, internal_folder,
nas_folder, smb_share), HardwareSerial, FilesystemUUID, FilesystemType, VolumeUUID,
VolumeLabel, Manufacturer, Model, CapacityBytes, ConnectionType, ContentFingerprint (JSON:
total file count, total bytes, representative path hash, representative content hash),
Confidence (0–100), ConfidenceReason (human-readable WHY), LastSeenAt, ImportCount,
SafeToErase (bool), SafeToEraseReason.

**BackupJob**: ID, AssetID (FK), Plugin, Destination, Status (enum: pending, running,
paused, completed, failed, cancelled), Retries, StartedAt, CompletedAt, ErrorMessage.

**BackupProvider**: ID, PluginName, ConfigJSON, Enabled.

**Setting**: Key (PK), ValueJSON.

**LogEntry**: ID (auto-increment int64 ok here), Timestamp, Level, Subsystem, Message,
MetadataJSON. Indexed on Timestamp, Level, Subsystem.

## Hashing & duplicate detection (internal/hashing, repo-assisted)

- **Quick hash**: BLAKE3 over: 8-byte little-endian file size ‖ first 4 MiB ‖ last 4 MiB.
  Files ≤ 8 MiB: 8-byte size ‖ entire content. Hex-encoded.
- **Full hash**: BLAKE3 of the entire file content (streamed, 1 MiB buffer). Hex-encoded.
- Duplicate detection: Stage 1 — look up QuickHash among non-deleted assets. No match →
  unique. Match → Stage 2: compute full BLAKE3 of BOTH files (backfill the existing asset's
  FullHash if null). **Only a full-hash match makes a duplicate.** Never use filenames,
  timestamps, or EXIF for identity.

## Verification & copy protocol (internal/importer)

For every imported file:
1. Compute quick hash of source (this also serves duplicate detection).
2. Copy to destination as a temp name `.paim-partial-<uuid>` in the final directory,
   `fsync` file and directory.
3. Verify: re-read the **destination** temp file and compare using the same BLAKE3 strategy
   (quick hash; full hash when it was computed for the source).
4. Atomic `rename` to final name. Name collision → append ` (2)`, ` (3)`, … before extension.
5. Only after successful verification: insert Asset with VerificationStatus=verified and
   enqueue backup jobs, all within one DB transaction with the session counter update.
6. Any failure: delete only the temp partial file (never a verified file), record failure,
   continue with the next file. Never mark success before verification.

Resume: on startup, sessions with status running/interrupted are marked `interrupted` and
resumable; re-running a session re-scans and skips files whose (QuickHash[, FullHash])
already exist as verified assets — so a crash mid-copy never duplicates imports. Stray
`.paim-partial-*` files under the destination root are deleted on resume (they were never
recorded as assets).

## Archive layout (internal/archive)

Default (user-configurable via Settings): `<MasterLibrary>/YYYY/YYYY-MM-DD Event/` with RAW
files in a `RAW/` subfolder; JPEGs and videos side by side. Date from CaptureDate, falling
back to file mtime (fallback is recorded in logs). "Event" name supplied per import session
(default: empty → folder is just `YYYY-MM-DD`).

## Media support (internal/mediatype)

Photos: jpg, jpeg, png, tiff, tif, heic, avif. RAW: raf, cr2, cr3, arw, nef, orf, rw2, dng.
Videos: mov, mp4, m4v, avi, mxf. Live Photos: a HEIC (or JPG) + MOV pair sharing the same
basename in the same source directory AND matching exiftool ContentIdentifier when
available — linked via LivePhotoPartnerID and treated as one logical asset (imported,
verified, backed up, and counted together).

## Metadata (internal/metadata)

exiftool in `-stay_open` batch mode, `-json -n` output, one persistent process, mutex-
guarded, restarted on failure. Extract: capture time (SubSecDateTimeOriginal >
DateTimeOriginal > CreateDate), camera make/model, lens, GPS, exposure, ISO, aperture,
dimensions, orientation, duration, frame rate, codec, color space, ContentIdentifier.
Stored in SQLite on import; never re-read EXIF for data we already have. If exiftool is
not installed: log prominently, degrade to zero-metadata import (capture date from mtime),
and surface the degradation in the UI status.

## Import source identification (internal/source, internal/volumes)

Volume labels (UNTITLED, NO NAME, EOS_DIGITAL…) are never identity. Collect per volume via
`diskutil info -plist <mount>` and `system_profiler SPUSBDataType -json` / `SPCardReaderDataType`:
hardware serial, filesystem UUID, volume UUID, filesystem type, capacity, manufacturer,
model, connection type. Compute a **content fingerprint**: total file count, total bytes,
representative path hash (BLAKE3 of the sorted relative paths of up to 200 evenly-sampled
media files), representative content hash (BLAKE3 of the quick hashes of up to 20 sampled
files).

Confidence model (score 0–100 with recorded reasons, e.g.):
- Hardware serial / volume UUID match → 100, "Hardware ID match — known device".
- Filesystem UUID + capacity + fs type match → 90.
- Content fingerprint match → 100 for contents, "Content match — previously imported".
- Partial matches score lower; every conclusion stores a human-readable WHY in
  ConfidenceReason / SafeToEraseReason.

`ImportSourceProvider` interface (sd_card, usb_ssd, external_hdd, internal_folder,
nas_folder, smb_share all satisfy it): `Type()`, `Identify(ctx, mountPoint) (*Identity, error)`,
`Scan(ctx, root, progressFn) ([]FileInfo, error)`. Never assume SD card.

**Safe to erase** is true only when: every media file on the volume maps to a verified
archived asset AND all required backups for those assets are complete AND DB status is
consistent. The reason string always explains the conclusion.

## Backup system (internal/backup)

Import and backup are separate; import completes first, backups run asynchronously from a
SQLite-persisted queue (BackupJob rows are the queue — restart-safe by construction).
Worker pool (configurable size, default 2). Jobs support pause/resume/retry/cancel;
exponential backoff on retry (max retries configurable, default 5). On startup, jobs left
`running` revert to `pending`.

`BackupPlugin` interface: `Name()`, `Initialize(ctx, configJSON)`, `Authenticate(ctx)`,
`Upload(ctx, localPath, remoteRelPath, progressFn) error`, `Delete(ctx, remoteRelPath)`,
`Verify(ctx, localPath, remoteRelPath) (bool, error)`, `Capabilities() Capabilities`.
First plugin: `localfs` (native Go copy + fsync + BLAKE3 verify, same temp-file/rename
protocol as import). Core never imports a concrete plugin except in main.go registration.

## Cleanup Assistant (internal/cleanup)

Read-only analysis of any folder: classify every media file as already_archived (full-hash
verified against archive), duplicate, new, unknown (non-media/unreadable), or
verification_failed. Summary drives a recommendation that follows the Safe Delete Rules:
recommend deletion ONLY if every asset is archived + verified + required backups complete.
Otherwise "Deletion NOT recommended" with counts and reasons.

## Logging (internal/logging)

Everything logged (copy, verify, import, hash, duplicate detection, backup, retry, failure,
delete, cleanup) through `slog` with a custom handler that writes console + buffered async
inserts into LogEntry. Subsystem field mandatory. Logs page supports search/filter/export
(export = JSON or CSV via a service method).

## Wails v3 conventions (alpha2.117 — API notes)

- Services: struct pointers registered in `main.go` `application.Options.Services` via
  `application.NewService(&services.XService{...})`. Exported methods become frontend
  bindings; first param may be `context.Context`.
- Events: `application.RegisterEvent[T]("name")` in an `init()`, emit with
  `app.Event.Emit("name", payload)`. Frontend: `import {Events} from "@wailsio/runtime"` →
  `Events.On("name", cb)`. Event payload types must be JSON-serializable structs.
- Bindings: regenerate with `wails3 generate bindings` (root). Generated under
  `frontend/bindings/github.com/autolinepro/paim/...`.
- Folder picker: use `application.OpenFileDialog().CanChooseDirectories(true)…` inside a
  service method (runs on the Go side).
- Progress: engines report via callback; services throttle to ≤10 events/sec and emit
  typed events (`import:progress`, `backup:progress`, `volume:changed`, `log:entry`).

### Service surface (internal/services) — one struct per page area

DashboardService, ImportService (SelectSource/Scan/DryRun/Start/Pause/Cancel/Resume),
SourcesService, HistoryService, DuplicateService, CleanupService, BackupService
(queue list/pause/resume/retry/cancel), ProviderService, LogService, SettingsService.
Methods return DTOs defined in `services` (not raw GORM models when it would leak
internals; simple models may pass through).

## Frontend conventions

- Dark mode default: `<html class="dark">`, Tailwind v4 `@import "tailwindcss";` in
  `src/style.css` with `@custom-variant dark`. Zinc-950 background, zinc-100 text,
  blue-600 accents. Modern macOS feel: generous whitespace, large progress bars,
  status badges (colored pill component), minimal chrome.
- TanStack Router with file-per-route in `src/routes/`, a root layout with the Sidebar
  (Dashboard, Import, Sources, Import History, Duplicate Manager, Cleanup Assistant,
  Backup Queue, Providers, Logs, Settings) using Heroicons outline icons.
- Tables via a shared `DataTable` wrapper around TanStack Table.
- All bindings imports come from `frontend/bindings/...`; never fetch/axios.
- Destructive actions (duplicate delete/move, cleanup delete) always go through
  `ConfirmDialog`.

## Code quality bar

- Interfaces at consumption points; constructor injection; no globals except the slog
  default logger.
- `context.Context` on all long-running operations; cancellation honored between files.
- Unit tests colocated (`*_test.go`) for hashing, duplicate detection, archive layout,
  import pipeline (with temp dirs), verification, source confidence, backup queue,
  cleanup classification, and repos (in-memory/temp SQLite).
- Structured errors: wrap with `%w`, include file paths and operation names.
- Every long-running operation reports progress.
- `go build ./...` and `go test ./...` must pass; frontend `npm run build` must pass.
